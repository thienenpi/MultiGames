import { ActivityIndicator, Text, TouchableOpacity, View } from 'react-native';
import React from 'react';
import { COLORS } from '../constants';

const SpyGameBanner = ({ styles, label, onPress, isValid, isLoading }) => {
    return (
        <View>
            <Text> Hello đây là banner</Text>
        </View>
    )
};
export default SpyGameBanner;
