import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
      alignItems: 'center',
      padding: 16,
    },
    title: {
      fontSize: 16,
      marginRight: 'auto',
    },
    infoContainer: {
      flexDirection: 'row',
      alignItems: 'center',
    },
    index: {
      fontSize: 16,
      marginRight: 8,
    },
    icon: {
        fontSize: 16,
        opacity: 0.5,
        color: 'gray',
    },
  });

export default styles;